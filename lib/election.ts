export type ElectionStatus="DRAFT"|"SCHEDULED"|"OPEN"|"CLOSED"|"RESULTS_PUBLISHED"|"ARCHIVED";export type Choice={positionId:string;candidateId:string|null;abstained:boolean};export function votingAllowed(status:ElectionStatus,start:Date,end:Date,now=new Date()){return status==="OPEN"&&now>=start&&now<=end}export function validateChoices(positions:string[],candidatePosition:Record<string,string>,choices:Choice[],allowAbstention=false){if(choices.length!==positions.length)return false;const seen=new Set<string>();for(const c of choices){if(seen.has(c.positionId)||!positions.includes(c.positionId))return false;seen.add(c.positionId);if(c.abstained){if(!allowAbstention||c.candidateId!==null)return false}else if(!c.candidateId||candidatePosition[c.candidateId]!==c.positionId)return false}return seen.size===positions.length}export function rankResults(rows:{candidate:string;votes:number}[]){return [...rows].sort((a,b)=>b.votes-a.votes).map((row,i,all)=>({...row,rank:1+all.filter(x=>x.votes>row.votes).length,tiedForFirst:row.votes===all[0]?.votes&&all.filter(x=>x.votes===row.votes).length>1}))}
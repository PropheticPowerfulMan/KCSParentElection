"use client";import {FormEvent,useState} from "react";import {Send,ShieldCheck} from "lucide-react";export function CandidacyForm(){const [opened,setOpened]=useState(false);function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const data=new FormData(e.currentTarget);const fields=[["First name / Prenom",data.get("first_name")],["Last name / Nom",data.get("last_name")],["Phone / Telephone",data.get("phone")],["Email",data.get("email")],["Student / Eleve",data.get("student_name")],["Class / Classe",data.get("student_class")],["Relationship / Lien",data.get("relationship")],["Position",data.get("position")],["Biography / Biographie",data.get("biography")],["Manifesto / Programme",data.get("manifesto")]];const position=String(data.get("position")||"");const body=["KCS PARENT ELECTION - OFFICIAL CANDIDACY","",...fields.map(([k,v])=>k+": "+String(v??"")),"","Consent: I authorize KCS to use this information solely to review and administer my election candidacy.","","Please attach the candidate photo before sending."].join("\n");window.location.href="mailto:kinshasachristianschool@gmail.com?subject="+encodeURIComponent("KCS Parent Election - Candidacy - "+position)+"&body="+encodeURIComponent(body);setOpened(true)}return <form className="card candidate-form" onSubmit={submit}><div className="form-intro"><ShieldCheck/><div><h2>Personal information</h2><p>Fields marked * are required. No information is sent to an intermediary.</p></div></div><div className="form-grid"><label>First name *<input className="field" name="first_name" required maxLength={80}/></label><label>Last name *<input className="field" name="last_name" required maxLength={80}/></label><label>Phone number *<input className="field" name="phone" type="tel" required maxLength={30}/></label><label>Email address *<input className="field" name="email" type="email" required maxLength={120}/></label><label>Student name *<input className="field" name="student_name" required maxLength={120}/></label><label>Student class *<input className="field" name="student_class" required maxLength={50}/></label><label>Relationship *<select className="field" name="relationship" required defaultValue=""><option value="" disabled>Select</option><option>Father</option><option>Mother</option><option>Guardian</option></select></label><label>Position sought *<select className="field" name="position" required defaultValue=""><option value="" disabled>Select</option><option>President</option><option>Secretary</option><option>Treasurer</option></select></label></div><label>Biography *<textarea className="field" name="biography" required rows={5} maxLength={1500}/></label><label>Manifesto / program *<textarea className="field" name="manifesto" required rows={7} maxLength={3000}/></label><div className="photo-note"><b>Candidate photo</b><span>For privacy and compatibility, attach the photo in the email window that opens.</span></div><label className="consent"><input type="checkbox" required name="privacy_consent"/><span>I authorize KCS to use this information solely to review and administer my election candidacy.</span></label><button className="btn btn-primary" type="submit"><Send size={18}/>Prepare email to KCS</button>{opened&&<p className="status-message">Your email application was opened. Attach your photo, verify the information, then press Send.</p>}<p className="privacy-note">Recipient: kinshasachristianschool@gmail.com. The application is sent only when you press Send in your email application.</p></form>}
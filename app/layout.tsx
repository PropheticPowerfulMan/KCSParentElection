import type {Metadata} from "next";import "./globals.css";export const metadata:Metadata={title:"KCS Parent Election",description:"Secure digital parent committee election platform",manifest:"/manifest.webmanifest"};export default function Layout({children}:{children:React.ReactNode}){return <html lang="en"><body>{children}</body></html>}
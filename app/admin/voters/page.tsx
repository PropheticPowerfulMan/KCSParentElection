"use client";import {useCallback,useEffect,useMemo,useState} from "react";import Link from "next/link";import {CheckCircle2,KeyRound,RefreshCw,Search,ShieldAlert,Users} from "lucide-react";import {createSupabaseBrowserClient} from "@/lib/supabase/client";type Child={first_name:string;middle_name:string|null;last_name:string;student_class:string};type Voter={id:string;voter_code:string;first_name:string;middle_name:string|null;last_name:string;phone:string|null;email:string|null;eligible:boolean;has_voted:boolean;voted_at:string|null;locked_until:string|null;voter_children:Child[]};export default function Voters(){const[rows,setRows]=useState<Voter[]>([]);const[query,setQuery]=useState("");const[loading,setLoading]=useState(true);const[error,setError]=useState("");const[credential,setCredential]=useState<{code:string;pin:string}|null>(null);const load=useCallback(async()=>{setLoading(true);const{data,error}=await createSupabaseBrowserClient().from("voters").select("id,voter_code,first_name,middle_name,last_name,phone,email,eligible,has_voted,voted_at,locked_until,voter_children(first_name,middle_name,last_name,student_class)").order("last_name");if(error)setError("Le registre officiel n’a pas pu être chargé.");else setRows((data??[]) as Voter[]);setLoading(false)},[]);useEffect(()=>{load()},[load]);const visible=useMemo(()=>{const q=query.toLocaleLowerCase();return rows.filter(v=>JSON.stringify(v).toLocaleLowerCase().includes(q))},[rows,query]);async function reissue(v:Voter){if(!confirm(`Réémettre un nouveau PIN pour ${v.first_name} ${v.last_name} ? L’ancien PIN sera immédiatement invalidé.`))return;const{data,error}=await createSupabaseBrowserClient().rpc("reissue_voter_pin",{p_voter_id:v.id});if(error)setError("Réémission impossible. Exécutez la migration 011 ou vérifiez les droits et l’état du scrutin.");else setCredential(data)}return <><div className="admin-title-row"><div><span className="eyebrow">Registre électoral</span><h1>Parents et tuteurs</h1><p>{rows.length} inscriptions réelles. Les PIN ne sont jamais stockés ni réaffichés en clair.</p></div><button className="btn btn-secondary" onClick={load}><RefreshCw size={18}/>Actualiser</button></div><div className="registry-toolbar"><label><Search/><input className="field" value={query} onChange={e=>setQuery(e.target.value)} placeholder="Nom, code, enfant ou classe"/></label><Link className="btn btn-secondary" href="/admin/import/">Gestion des identifiants</Link></div>{error&&<p className="status-message error">{error}</p>}{credential&&<section className="credential-once card"><ShieldAlert/><div><b>À remettre confidentiellement — affichage unique</b><p>Code : <strong>{credential.code}</strong> · PIN : <strong>{credential.pin}</strong></p><small>Copiez ou imprimez maintenant. Le PIN ne pourra plus être récupéré.</small></div><button onClick={()=>setCredential(null)} className="btn btn-secondary">Masquer</button></section>}{loading?<section className="card empty-state"><RefreshCw className="spin"/></section>:<div className="registry-table-wrap card"><table className="registry-table"><thead><tr><th>Parent</th><th>Code électeur</th><th>Enfant(s)</th><th>État</th><th>Action</th></tr></thead><tbody>{visible.map(v=><tr key={v.id}><td><b>{[v.first_name,v.middle_name,v.last_name].filter(Boolean).join(" ")}</b><small>{v.phone||v.email||"Contact non renseigné"}</small></td><td><code>{v.voter_code}</code></td><td>{v.voter_children.map((c,i)=><small key={i}>{[c.first_name,c.middle_name,c.last_name].filter(Boolean).join(" ")} — {c.student_class}</small>)}</td><td><span className={`application-status ${v.has_voted?"approved":v.eligible?"pending":"rejected"}`}>{v.has_voted?"A voté":v.eligible?"Éligible":"Suspendu"}</span></td><td><button className="icon-action" disabled={v.has_voted} onClick={()=>reissue(v)} title="Réémettre le PIN" aria-label={`Réémettre le PIN de ${v.first_name}`}><KeyRound/></button></td></tr>)}</tbody></table>{!visible.length&&<div className="empty-state"><Users/><p>Aucun parent ne correspond à la recherche.</p></div>}</div>}</>}

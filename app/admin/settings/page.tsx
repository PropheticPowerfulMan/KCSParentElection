import {KeyRound,Languages,LockKeyhole,Palette,ShieldCheck} from "lucide-react";export default function Page(){const items=[[ShieldCheck,"Résultats intermédiaires","Toujours masqués jusqu’à la clôture officielle."],[KeyRound,"Authentification","Code électeur unique et PIN chiffré par bcrypt."],[LockKeyhole,"Protection anti-abus","Verrouillage temporaire après cinq échecs et session à usage unique."],[Languages,"Langues","Français par défaut, anglais disponible dans toute l’application."],[Palette,"Identité","Logo KCS et badge jaune E réservé à l’application Election."] ] as const;return <><span className="eyebrow">Paramètres système</span><h1>Politique de sécurité active</h1><p>Ces réglages sont imposés par le système et ne sont pas de simples commandes visuelles.</p><div className="grid-auto policy-grid">{items.map(([Icon,title,text])=><article className="card" key={title}><Icon/><h2>{title}</h2><p>{text}</p></article>)}</div></>}

import {Header} from "@/components/brand";import {candidates,positions} from "@/lib/data";import {Trophy,Vote} from "lucide-react";export default function Results(){const hasResults=positions.some(p=>candidates[p].length>0);return <><Header/><main className="shell" style={{padding:"54px 0"}}><div style={{textAlign:"center"}}><span className="eyebrow">Official published results</span><h1 style={{fontSize:"3rem",margin:"10px"}}>KCS Parent Election</h1></div>{!hasResults?<section className="card empty-state"><Vote/><h2>No results have been published</h2><p>The official election data will appear here after configuration, voting, certification, and publication by KCS.</p></section>:<div style={{display:"grid",gap:22,marginTop:35}}>{positions.map(p=><section className="card" style={{padding:25}} key={p}><h2 style={{display:"flex",gap:10,alignItems:"center"}}><Trophy color="var(--kcs)"/>{p}</h2><div style={{overflow:"auto"}}><table><thead><tr><th>Rank</th><th>Candidate</th><th>Votes</th><th>Percentage</th></tr></thead><tbody>{candidates[p].map((c,i)=><tr key={c[0]}><td>{i+1}</td><td>{c[1]}</td><td>0</td><td>0%</td></tr>)}</tbody></table></div></section>)}</div>}</main></>}
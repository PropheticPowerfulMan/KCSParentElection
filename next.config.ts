import type {NextConfig} from "next";const onProjectPath=process.env.GITHUB_ACTIONS==="true"&&process.env.KCS_CUSTOM_DOMAIN!=="true";const basePath=onProjectPath?"/KCSParentElection":"";const config:NextConfig={output:"export",trailingSlash:true,images:{unoptimized:true},basePath,assetPrefix:basePath,reactStrictMode:true,poweredByHeader:false};export default config;
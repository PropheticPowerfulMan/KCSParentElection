import type {NextConfig} from "next";const onProjectPath=process.env.GITHUB_ACTIONS==="true"&&process.env.KCS_CUSTOM_DOMAIN!=="true";const basePath=onProjectPath?"/KCSParentElection":"";const config:NextConfig={output:"export",trailingSlash:true,images:{unoptimized:true},basePath,assetPrefix:basePath,reactStrictMode:true,poweredByHeader:false,webpack(config){if(process.env.KCS_DISABLE_WEBPACK_CACHE==="true")config.cache=false;return config}};export default config;